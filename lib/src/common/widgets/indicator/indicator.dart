import 'package:flutter/material.dart';

@immutable
final class SPCircularIndicator extends StatelessWidget {
  const SPCircularIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
